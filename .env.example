# Local dev: `npx convex dev` auto-generates .env.local with the URL
# Production: set this to your Convex deployment URL
# Default dev URL (used in .env if .env.local is absent):
VITE_CONVEX_URL=http://localhost:8188
